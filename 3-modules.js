// Common JS, every file is module by default (by default)
// Modules - Encapsulated Code (only share minimum)

// MODULES
const names = require("./4-names")
const sayHi = require("./5-utils")
require("./6-number")

// sayHi("susan");
// sayHi(names.john);
// sayHi(names.peter);

// //